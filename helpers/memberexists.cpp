//linear search method to confirm that member exsits

#include<iostream>
#include<fstream>
#include<string>

using namespace std;

bool memberexists(const char *filename){
        std::ifstream ifile(filename);
        }
